# compiler

# group of statements to a single line
#   - greedy approach or most lines reduced

# single line if / else / while blocks on the same line as the condition

# non-recursive function calls dont need stack?
#   - variables must be renamed to be unique from global vars
